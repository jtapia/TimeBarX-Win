<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="TimeBarX.App.QuickInputWindow"
        WindowStartupLocation="CenterScreen"
        WindowDecorations="None"
        Topmost="True"
        ShowInTaskbar="False"
        CanResize="False"
        SizeToContent="WidthAndHeight"
        Width="420"
        Background="Transparent"
        TransparencyLevelHint="Transparent"
        Title="TimeBarX — Quick Timer">
    <!-- Window is transparent so only this rounded Border paints — otherwise the
         square window background pokes out behind the corners. The Border still
         inherits theme colors so it follows system light/dark. -->
    <Border CornerRadius="8"
            Margin="1"
            Padding="16"
            BorderThickness="1"
            Background="{DynamicResource SystemRegionBrush}"
            BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">
        <StackPanel Spacing="8">
            <TextBlock Text="Start a timer"
                       Opacity="0.7"
                       FontSize="12" />
            <TextBox Name="InputBox"
                     FontSize="20"
                     PlaceholderText="25 min, 1:30, 2h review PR…"
                     KeyDown="OnKeyDown" />
            <TextBlock Name="HintText"
                       Text="Enter to start  ·  Esc to cancel"
                       Opacity="0.55"
                       FontSize="11" />
        </StackPanel>
    </Border>
</Window>
